kafka:
  #//Uncomment to use embedded kafka broker
  #bootstrap-servers: ${spring.embedded.kafka.brokers}
  bootstrap-servers: localhost:9092
  topic:
    c3topic: c3testtopic
    jsontopic: jsontesttopic